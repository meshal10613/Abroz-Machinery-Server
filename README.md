# Docker Commands (Abroz Machinery Project)

## 🚀 Start services with multiple app instances

```bash
docker compose up -d --scale app=5 --remove-orphans
```

## 🛑 Stop all running containers

```bash
docker compose down
```